🚀 Early Return Pattern (JavaScript)
❓ What is Early Return?

👉 Early return means
exit a function as soon as a condition is met,
instead of using deep if–else blocks.

📌 Improves:

Readability

Performance

Maintainability

❌ Without Early Return (Bad Practice)
function checkAge(age) {
  if (age >= 18) {
    return "Eligible";
  } else {
    return "Not Eligible";
  }
}


✔ Works
❌ But unnecessary else

✅ With Early Return (Good Practice)
function checkAge(age) {
  if (age < 18) {
    return "Not Eligible";
  }
  return "Eligible";
}

✅ Output
console.log(checkAge(16)); // Not Eligible
console.log(checkAge(21)); // Eligible

📌 Example 2: Input Validation (REAL USE CASE)
❌ Without Early Return
function login(username, password) {
  if (username) {
    if (password) {
      if (password.length >= 4) {
        return "Login Success";
      } else {
        return "Password too short";
      }
    } else {
      return "Password required";
    }
  } else {
    return "Username required";
  }
}


❌ Hard to read
❌ Too many nested if

✅ With Early Return (BEST PRACTICE)
function login(username, password) {
  if (!username) return "Username required";
  if (!password) return "Password required";
  if (password.length < 4) return "Password too short";

  return "Login Success";
}

✅ Output
console.log(login("", "1234"));      // Username required
console.log(login("admin", ""));     // Password required
console.log(login("admin", "12"));   // Password too short
console.log(login("admin", "1234")); // Login Success

📌 Example 3: Loop + Early Return
function findNumber(arr, target) {
  for (let num of arr) {
    if (num === target) {
      return "Found";
    }
  }
  return "Not Found";
}

✅ Output
console.log(findNumber([1, 2, 3, 4], 3)); // Found
console.log(findNumber([1, 2, 3, 4], 6)); // Not Found

🔑 Why Early Return is Important?
Without Early Return	With Early Return
Deep nesting	Flat structure
Hard to debug	Easy to read
More bugs	Cleaner logic
🧠 Interview One-Liner

Early return is a pattern where a function exits immediately when a condition is met, reducing nesting and improving readability.

✅ When to Use Early Return

✔ Validation checks
✔ Error handling
✔ Search operations
✔ Guard clauses