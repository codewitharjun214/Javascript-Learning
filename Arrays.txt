📦 JavaScript Arrays

An array is used to store multiple values in one variable.

1️⃣ Creating an Array
let fruits = ["Apple", "Banana", "Mango"];
console.log(fruits);

✅ Output
[ 'Apple', 'Banana', 'Mango' ]

2️⃣ Accessing Elements (Index)
console.log(fruits[0]);
console.log(fruits[2]);

✅ Output
Apple
Mango


📌 Index starts from 0

3️⃣ Changing Elements
fruits[1] = "Orange";
console.log(fruits);

✅ Output
[ 'Apple', 'Orange', 'Mango' ]

4️⃣ Array Length
console.log(fruits.length);

✅ Output
3

5️⃣ Adding & Removing Elements
➕ push() – add at end
fruits.push("Grapes");
console.log(fruits);

✅ Output
[ 'Apple', 'Orange', 'Mango', 'Grapes' ]

➖ pop() – remove from end
fruits.pop();
console.log(fruits);

✅ Output
[ 'Apple', 'Orange', 'Mango' ]

➕ unshift() – add at start
fruits.unshift("Papaya");
console.log(fruits);

✅ Output
[ 'Papaya', 'Apple', 'Orange', 'Mango' ]

➖ shift() – remove from start
fruits.shift();
console.log(fruits);

✅ Output
[ 'Apple', 'Orange', 'Mango' ]

6️⃣ Looping Through Arrays
for loop
for (let i = 0; i < fruits.length; i++) {
  console.log(fruits[i]);
}

✅ Output
Apple
Orange
Mango

for...of
for (let fruit of fruits) {
  console.log(fruit);
}

7️⃣ Important Array Methods (🔥🔥)
🔹 forEach() (no return)
fruits.forEach((item, index) => {
  console.log(index, item);
});

✅ Output
0 Apple
1 Orange
2 Mango

🔹 map() (returns new array)
let nums = [1, 2, 3];

let doubled = nums.map(n => n * 2);
console.log(doubled);

✅ Output
[ 2, 4, 6 ]

🔹 filter() (condition based)
let numbers = [10, 15, 20, 25];

let result = numbers.filter(n => n > 15);
console.log(result);

✅ Output
[ 20, 25 ]

🔹 reduce() (single value)
let total = [1, 2, 3, 4].reduce((sum, n) => sum + n, 0);
console.log(total);

✅ Output
10

8️⃣ Searching in Arrays
🔍 includes()
console.log(fruits.includes("Apple"));

✅ Output
true

🔍 indexOf()
console.log(fruits.indexOf("Mango"));

✅ Output
2

9️⃣ slice() vs splice() (VERY IMPORTANT ⚠️)
🔹 slice() – does NOT change original
let arr = [1, 2, 3, 4];

let sliced = arr.slice(1, 3);
console.log(sliced);
console.log(arr);

✅ Output
[ 2, 3 ]
[ 1, 2, 3, 4 ]

🔹 splice() – changes original
let a = [1, 2, 3, 4];

a.splice(1, 2);
console.log(a);

✅ Output
[ 1, 4 ]

🔟 Array Destructuring (ES6)
let [first, second] = fruits;

console.log(first);
console.log(second);

✅ Output
Apple
Orange

🧠 Interview Tips

✔ map, filter, reduce are MOST important
✔ forEach does not return
✔ slice ≠ splice
✔ Arrays are reference type